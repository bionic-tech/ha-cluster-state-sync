{
  "config": {
    "step": {
      "user": {
        "title": "Before you install",
        "description": "**This is alpha software and there are no guarantees that it runs.** It is shared with the Home Assistant community for testing and feedback, and no guarantees are given on data integrity or on consistency of functionality at this time.\n\n**It restores state to devices that act in the physical world.** The default mirrored domains include `climate`, `water_heater`, `vacuum` and `timer` — so a stale or wrong restore can change your heating setpoint or hot water, not just a value on a dashboard. `alarm_control_panel`, `person` and `device_tracker` are **opt-in and off by default**; if you turn them on, an alarm's state is mirrored too.\n\nProvided as is, without warranty and without liability. Prove a failover on a system you do not depend on, before you depend on one.",
        "data": {
          "accept_risk": "I understand and accept the risk"
        }
      },
      "direct": {
        "title": "Direct Redis / Valkey connection",
        "data": {
          "redis_host": "Host",
          "redis_port": "Port",
          "redis_username": "Username (leave blank if the server has no ACL user)",
          "redis_password": "Password (leave blank if none)",
          "redis_db": "Database number",
          "cluster_namespace": "Cluster namespace (must match across nodes)",
          "node_id": "This node's identifier",
          "cluster_secret": "Cluster secret (must be identical on both nodes)",
          "redis_use_tls": "Use TLS",
          "redis_tls_ca_certs": "TLS CA certificate path (blank = system trust store)",
          "snapshot_interval": "Snapshot interval (seconds)",
          "restore_max_age": "Maximum snapshot age to restore (seconds)"
        },
        "data_description": {
          "cluster_secret": "Signs every snapshot entry so a node only accepts state written by its peer. Copy this exact value to the other node — a mismatch means neither will restore from the other. Without it, nothing is restored at all.",
          "redis_use_tls": "Strongly recommended if you mirror person, device_tracker or alarm_control_panel entities, which otherwise cross the network in the clear."
        }
      },
      "sentinel": {
        "title": "Sentinel-backed Redis / Valkey",
        "data": {
          "redis_sentinel_hosts": "Sentinel hosts (comma-separated host:port)",
          "redis_sentinel_service": "Sentinel master service name",
          "redis_username": "Username (leave blank if the server has no ACL user)",
          "redis_password": "Password (leave blank if none)",
          "redis_db": "Database number",
          "cluster_namespace": "Cluster namespace (must match across nodes)",
          "node_id": "This node's identifier",
          "cluster_secret": "Cluster secret (must be identical on both nodes)",
          "redis_use_tls": "Use TLS",
          "redis_tls_ca_certs": "TLS CA certificate path (blank = system trust store)",
          "snapshot_interval": "Snapshot interval (seconds)",
          "restore_max_age": "Maximum snapshot age to restore (seconds)"
        },
        "data_description": {
          "cluster_secret": "Signs every snapshot entry so a node only accepts state written by its peer. Copy this exact value to the other node — a mismatch means neither will restore from the other. Without it, nothing is restored at all.",
          "redis_use_tls": "Strongly recommended if you mirror person, device_tracker or alarm_control_panel entities, which otherwise cross the network in the clear."
        }
      },
      "topology": {
        "title": "Standby model",
        "description": "Cold is recommended: the standby's Home Assistant is stopped, so there are no side effects to suppress. Warm fails over in seconds but needs the firewall bundle.",
        "data": {
          "topology_model": "Standby model",
          "leadership_source": "Leadership signal",
          "leadership_entity": "Leadership entity (only for the 'entity' signal)",
          "gate_recorder": "Stop recorder writes on the follower (needs a shared recorder database)",
          "gate_automations": "Stop automations on the follower (warm standby only)",
          "ha_config_path": "Home Assistant config path ON THE HOST (not /config)",
          "ha_container": "Home Assistant container name"
        },
        "data_description": {
          "leadership_source": "Only the leader writes to the shared snapshot. 'always' suits a single node or a cold standby whose HA is stopped. 'entity' follows a flag your Keepalived notify_master/notify_backup scripts toggle. 'lease' takes a TTL lease in Valkey and is the strongest option — it is the split-brain guard, since VRRP alone can dual-master under a partition."
        }
      },
      "warm": {
        "title": "Warm standby — network gating",
        "description": "Used to generate the nftables/iptables rules the follower loads. These rules are generated without access to your hosts and are not validated — read them before loading.",
        "data": {
          "iot_subnets": "IoT subnet(s), comma-separated CIDR",
          "block_discovery": "Block mDNS (5353) and SSDP (1900) on the follower",
          "docker_network": "Docker network mode",
          "ha_uid": "UID Home Assistant runs as",
          "ha_container_ip": "Container IP (macvlan/bridge only)",
          "settle_delay": "Promotion settle delay (seconds)"
        }
      },
      "fileset": {
        "title": "Config replication",
        "description": "Everything `configuration.yaml` includes is replicated automatically — `configs/`, `themes/`, `packages/` and so on are followed, so you do not list them here.\n\nThis is for what no parser can see: files an integration opens by path at runtime. Anything found in your config directory is offered below; you can also type a path.",
        "data": {
          "fileset_enabled": "Replicate the config fileset to the standby",
          "fileset_hot_interval": "How often to publish .storage (seconds)",
          "fileset_exclusions": "Filename patterns to skip",
          "fileset_extra_paths": "Also replicate these (tick what a promoted standby needs)",
          "fileset_max_bytes": "Refuse to publish above this many bytes",
          "fileset_stale_after": "Warn if the standby's copy is older than (seconds)",
          "fileset_extra_custom": "Additional paths not listed above (type and press enter)"
        }
      },
      "bundle": {
        "title": "Host bundle generated",
        "description": "Written to:\n`{bundle_path}`\n\nModel: **{model}**\n\n{file_list}\n\n{next_steps}",
        "menu_options": {
          "transfer": "Show me how to copy these onto this host",
          "finish": "Finish — I'll handle the files myself"
        }
      },
      "choose": {
        "title": "How does this node reach Valkey?",
        "description": "**Direct** is what almost everyone wants: one Valkey or Redis server, reached by host and port.\n\n**Sentinel** is for a Redis Sentinel cluster that elects its own primary. The code path exists but is **deferred and untested** — choose it only if you already run Sentinel and are willing to be the first to prove it.",
        "menu_options": {
          "direct": "Direct — a single Valkey or Redis server",
          "sentinel": "Sentinel — a Redis Sentinel cluster (untested)"
        }
      },
      "transfer": {
        "title": "Copying the bundle onto this host",
        "description": "The bundle was written **inside** Home Assistant's container. It has to reach `/etc/cluster-sync` on the host this container runs on.\n\nLeave all three blank if you are already at a terminal on that host.\n\n🚨 This is about **this** node only. Nine of these files carry this node's identity — installing them on the other node would make it claim this one's id, and both would then believe they lead. The other node runs its own wizard.\n\nNone of these answers is saved.",
        "data": {
          "host": "Host address (blank if you are on it already)",
          "user": "SSH username (blank for your current one)",
          "key_path": "SSH key path (blank for your default, or if you use a password)"
        }
      },
      "transfer_commands": {
        "title": "Commands to run",
        "description": "{commands}\n\nSubmit to go back."
      },
      "domains": {
        "title": "What crosses to the standby",
        "description": "Which entity domains are mirrored and restored on the node that takes over.\n\nThe defaults are conservative on purpose: stateful things a promoted node needs to know, and nothing whose restore could actuate hardware.\n\n**Adding `light` or `switch` does not do what it looks like.** Verified against Home Assistant's own source: the restore runs on `EVENT_HOMEASSISTANT_START`, and an automation attaches its triggers on `EVENT_HOMEASSISTANT_STARTED` — which fires *after*. So a restored value at boot cannot fire your automations; they are not listening yet. That is Home Assistant's own behaviour and this integration deliberately matches it rather than working around it.\n\n`person`, `device_tracker` and `alarm_control_panel` also tell anyone who can read the shared store when the house is empty. Pair them with TLS and a dedicated user.\n\n🚨 **And for `light`, `switch` and other device-backed domains the restore does not actually work.** It writes Home Assistant's state machine; it does not command the device. The integration that owns the light still believes what it believed, and corrects the value the next time it reports — so you get a reading that flips, fires your automations on the way past, and then reverts. The risk without the benefit.\n\n`automation` is the exception, and only because it is applied by calling `automation.turn_on`/`turn_off` rather than by writing state. Extending that to `light` or `switch` would mean a promotion switching real things on in your house, which is a decision this integration will not make for you.",
        "data": {
          "include_domains": "Entity domains to mirror",
          "include_entities": "Individual entities to mirror as well (optional)"
        },
        "data_description": {
          "include_domains": "Leave empty to use the built-in defaults.",
          "include_entities": "Full entity IDs, for one-off entities in a domain you do not want wholesale. They need not exist yet."
        }
      },
      "container": {
        "title": "This node's runtime and radios",
        "description": "**`docker` — the default, and right for most installs.** The promoter runs `docker start` / `docker stop` on one container. Fast, needs no project file, and nothing else in your estate can break it.\n\n**`compose` — when nobody starts the container by hand.** Home Assistant is one service in a compose project, possibly behind a profile. The promoter acts on that service alone (`up -d --no-deps`), never the whole estate, and stops it with `stop` — never `down`, which would remove the container a demoted node needs in order to be promoted again.\n\n🚨 **Compose mode needs an env file.** Compose interpolates the *entire* project file before it filters by profile or service, so one unset variable in a service you are not touching aborts the command. The promoter runs from systemd with none of your shell environment — without an env file it fails at exactly the moment it is needed.\n\n**Radio liveness.** The promotion probe asks Home Assistant whether it is alive, and Home Assistant can be alive while every radio behind it is dead — that happened here, with a Zigbee daemon livelocked for 32 minutes while its healthcheck said `healthy`. Naming entities that update on reception makes the silence visible. It does **not** trigger failover: promoting because a radio died would move your house to a node whose radios may be no better.",
        "data": {
          "ha_start_mode": "Start method",
          "compose_file": "Compose file",
          "compose_service": "Service name",
          "compose_profile": "Profile (if the service is behind one)",
          "compose_env_file": "Env file to source before every compose call",
          "radio_watch": "Entities that prove a radio is receiving"
        },
        "data_description": {
          "compose_file": "Full path on the host, e.g. /srv/docker/compose.yml",
          "compose_service": "The service name in the file, not the container name.",
          "compose_profile": "Leave empty if you do not use profiles.",
          "compose_env_file": "Strongly recommended. Sourced and exported before compose runs.",
          "radio_watch": "Globs, e.g. sensor.*_rssi_numeric. Creates a 'radio silence' sensor showing seconds since any of them was last heard from. ⚠ Watch ONE radio's signals — the sensor reports the FRESHEST match, so mixing a Wi-Fi RSSI in with your Zigbee or RF sensors means the Wi-Fi masks the dead radio. Check the sensor's 'status' attribute, not just its value: 'no_reports' means the entities exist but no radio has ever been heard from, and that reads as unknown rather than a big number. Leave empty to skip."
        }
      },
      "history": {
        "title": "Your history, and where it lives",
        "description": "Home Assistant keeps a **history database** — the record of what happened and when: that you switched the kitchen light on at 19:04 last Tuesday, that an automation ran, that the temperature fell overnight. It is what every graph and the logbook are drawn from.\n\nEach machine normally keeps its **own** copy, so if the other one takes over, its graphs start blank from the day it was built.\n\n**Shared database** — both machines write to one PostgreSQL or MariaDB. Nothing to copy, history is simply continuous, and it works with either standby model. See Home Assistant's own guide: {recorder_docs}\n\n**Dedicated database** — each machine keeps its own SQLite file. Simpler, nothing extra to run. History can still follow a failover, but **only with cold standby**: swapping the file requires Home Assistant to be stopped, so a warm node cannot do it without a restart — which would make it slower than cold.",
        "data": {
          "history_matters": "Should history survive a failover?",
          "history_database": "Where does the history database live?"
        },
        "data_description": {
          "history_matters": "Say no if you only care that the house keeps working and do not mind a gap in the graphs.",
          "history_database": "This choice decides which standby models you are offered on the next screen."
        }
      },
      "statistics": {
        "title": "History that survives a failover",
        "description": "Only **long-term statistics** cross between nodes — the years of energy and climate data behind the Energy dashboard and long-range graphs. On this kind of estate that is about half a megabyte a day.\n\nRecent raw history — the logbook, the last ten days of detail — does **not** cross. It churns roughly sixty times faster, and replicating it was measured at 4 GB a day. After a failover your long graphs are intact and the logbook starts fresh.\n\n**One manual step.** Years of existing statistics cannot arrive half a megabyte at a time, so the standby needs a one-off seed file that you copy across yourself (on an estate this size, about 500 MB — a few seconds over a wired network). Once this is set up, press **Write statistics seed** on the Cluster page and follow the instructions it gives you.\n\nThis uses the same encryption key as the config replication, so leave that switched on.",
        "data": {
          "statistics_enabled": "Replicate long-term statistics",
          "statistics_window_days": "How far back each update reaches",
          "statistics_interval_minutes": "How often to publish"
        },
        "data_description": {
          "statistics_window_days": "Also how long the standby may be switched off and still catch up completely. 30 days costs about 4.5 MB per update; 90 days about 12.5 MB.",
          "statistics_interval_minutes": "How much history a failover can lose. Home Assistant compiles statistics every five minutes, so anything faster than that republishes the same data."
        }
      },
      "alerts": {
        "title": "Who hears about it",
        "description": "Every failure this integration can detect is, so far, visible only to somebody **looking** at Home Assistant. For a cluster whose whole point is that the house keeps working while you are away, that stops one step short.\n\n**You can submit this page untouched.** The defaults raise a persistent notification — which every administrator sees — for the four conditions that change whether the house is protected *right now*. The statistics conditions are left off deliberately: they can wait until Saturday, and an alarm that cries wolf is one nobody believes at 3am.\n\n**Adding a notify service is what reaches you when you are out.** Any `notify.` service works — the Home Assistant mobile app, Discord, Slack, Telegram, email. Home Assistant already holds those credentials; this integration never sees them and stores only the service name.\n\nYou can type a service that does not exist yet — a phone you have not paired, for instance. A name that never resolves costs an alert and a warning in the log; it can never stop the cluster.",
        "data": {
          "notify_conditions": "Tell me about",
          "notify_services": "Send to (optional)"
        },
        "data_description": {
          "notify_conditions": "Unticking a condition silences the push only. The repairs card and the diagnostic entities are always raised, so nothing is hidden — you just are not interrupted.",
          "notify_services": "Leave empty and alerts still reach every administrator as a persistent notification inside Home Assistant."
        }
      },
      "ingress": {
        "title": "Can anyone still get in?",
        "description": "**Your cluster can fail over perfectly and still leave you locked out.** That is not hypothetical: it happened on the reference pair. The lease moved, the radios followed, Home Assistant came up healthy and ran the house — and the address on the phone still pointed at the machine that had died and returned **502**. Every check the cluster performed was green, because every check the cluster performed looked inwards.\n\nGive it the address **you** type, and whichever node holds the lease will request it once a minute and tell you when it stops answering.\n\n### 🚨 What a green reading does not prove\n\nThe request is made from inside Home Assistant, on the node itself, on your own network. So:\n\n- **It cannot prove somebody outside can reach you.** If you use split-horizon DNS — normal for anyone reaching Home Assistant through a tunnel — this hostname resolves to a local address from in here and to your tunnel from a phone on mobile data. The check then tests a path no outside user ever takes, and stays green while the tunnel is down. No probe launched from inside a network can test the path that starts outside it. For that half, use an external uptime monitor.\n- **It cannot prove *this* node answered.** Something at that address responded. Behind a proxy, or in a warm-standby pair, that may have been the other node.\n- **It cannot prove you can log in.** Redirects are not followed, so an SSO challenge counts as the door working.\n\nA **red** reading is the trustworthy direction: if this cannot reach the address from a machine on the same network as the service, nobody further away is getting in either.\n\nNone of the infrastructure — DNS, proxies, tunnels, floating addresses — is moved by this integration, and it never will be. See `GUIDE-ingress.md`, which carries its own no-warranty notice. This page only makes the cluster **look**.",
        "data": {
          "ingress_url": "Your Home Assistant address",
          "ingress_verify_tls": "Check the certificate"
        },
        "data_description": {
          "ingress_url": "The whole address including the scheme, as in {example_url}. Leave it empty to switch the check off entirely — that is the default, and no entity or alert is created. 401 and 403 count as reachable: being refused proves something is there to refuse you.",
          "ingress_verify_tls": "Turn this off if you terminate TLS on a certificate Home Assistant has no reason to trust — an internal CA or a self-signed one. The check still answers “did anything answer at that address”; it just stops asking who. Better than a check that is red for ever and gets switched off along with the real alarm."
        }
      }
    },
    "error": {
      "cannot_connect": "Could not reach the backend with these settings.",
      "invalid_namespace": "Namespace must be lowercase letters, digits, '_' or '-' only.",
      "fileset_needs_secret": "A cluster secret is required to encrypt the replicated fileset. Set one on the backend step before enabling this.",
      "must_accept": "You must accept before continuing. This software carries no warranty.",
      "unsafe_shell_value": "One of these values contains a character that cannot be used here — most likely a quote, a space, a backtick, or one of ; & | $ ( ). These values are written into scripts this host runs as root, so anything that could change what those scripts do is refused. Check the container name, config path, Valkey host, username and peer address.",
      "invalid_ingress_url": "That is not an address this can request. It needs both a scheme and a host name — the field description above shows the shape."
    },
    "abort": {
      "already_configured": "This node is already configured for this cluster namespace.",
      "reconfigure_successful": "Cluster State Sync reconfigured. The host bundle has been regenerated — reinstall it on this host if any host-side value changed."
    }
  },
  "issues": {
    "fileset_degraded": {
      "title": "Standby config fileset is degraded",
      "description": "The host-side swap could not put a fresh copy of the config fileset in place before this instance started (reason: {reason}). Decision D4 promotes anyway rather than refuse to boot, so Home Assistant is running, but integrations, devices, and the mobile app's login may be missing or out of date. Check the swap log on the host, and reload this integration once a fresh fileset has been staged and swapped in."
    },
    "statistics_schema_mismatch": {
      "title": "Standby cannot store history: recorder schema mismatch",
      "description": "The standby refused the replicated long-term statistics because its recorder schema is a different version from this node's ({detail}). It refuses rather than guessing, because writing rows shaped for one schema into another corrupts history in a way that opens cleanly and is wrong. Its history has stopped advancing and will have a hole from now until this is fixed. Bring both nodes to the same Home Assistant version, then start the standby's Home Assistant once so its recorder migrates."
    },
    "statistics_gap": {
      "title": "Standby's history has a gap that will not close",
      "description": "The standby has been out of contact for longer than the statistics window reaches back, so the history in between is on neither node and no later window will contain it ({detail}). Everything from here forward will replicate normally. To recover the missing stretch, widen the statistics window and wait one publish, or write a fresh seed and copy it across."
    },
    "statistics_not_seeded": {
      "title": "Standby has no history to replicate into",
      "description": "Long-term statistics are being published, but the standby has nowhere to put them ({detail}). Years of statistics cannot arrive through the window — it carries about half a megabyte a day — so the standby needs a one-off seed. Use the 'Write statistics seed' button, copy the file it names to the same path on the standby, and the next pull there will adopt it automatically."
    },
    "statistics_stalled": {
      "title": "Standby's history replication has gone quiet",
      "description": "The standby has not reported applying long-term statistics for several publish intervals ({detail})\n\nThis alarm exists because silence used to be indistinguishable from success: the standby's status expires, the warnings clear themselves, and replication that had stopped looked exactly like replication that was working."
    },
    "devices_disabled": {
      "title": "Promoted without {count} device(s)",
      "description": "This node started with **{count} config entry(s) switched off** because their hardware is not attached here: {detail}\n\nThat is deliberate. A node missing a radio is more use than one whose integrations fail at setup and stay failed until somebody restarts it by hand — so the promotion pre-flight disables them and lets the node come up.\n\n**But whatever those devices do is not happening.** If this node is expected to have that hardware, check the device path: it usually embeds the unit's serial number, so a *different* physical device on this machine will not match the path the other node wrote. A stable `udev` symlink pointing at whichever unit is present on each host is the usual answer.\n\nRe-enable the entries under **Settings → Devices & services** once the hardware is there. This message clears itself on the next promotion that finds everything."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Cluster State Sync options",
        "menu_options": {
          "connection": "Backend connection — where Valkey is and how to reach it",
          "alerts": "Alerts — who hears about a failover, and how",
          "replication": "Replication — which entities and devices cross to the standby",
          "ingress": "Front door — whether the address you type still reaches this cluster"
        }
      },
      "connection": {
        "title": "Backend connection"
      },
      "alerts": {
        "title": "Who hears about it",
        "description": "Every failure this integration can detect is, so far, visible only to somebody **looking** at Home Assistant. For a cluster whose whole point is that the house keeps working while you are away, that stops one step short.\n\n**You can submit this page untouched.** The defaults raise a persistent notification — which every administrator sees — for the four conditions that change whether the house is protected *right now*. The statistics conditions are left off deliberately: they can wait until Saturday, and an alarm that cries wolf is one nobody believes at 3am.\n\n**Adding a notify service is what reaches you when you are out.** Any `notify.` service works — the Home Assistant mobile app, Discord, Slack, Telegram, email. Home Assistant already holds those credentials; this integration never sees them and stores only the service name.\n\nYou can type a service that does not exist yet — a phone you have not paired, for instance. A name that never resolves costs an alert and a warning in the log; it can never stop the cluster.",
        "data": {
          "notify_conditions": "Tell me about",
          "notify_services": "Send to (optional)"
        },
        "data_description": {
          "notify_conditions": "Unticking a condition silences the push only. The repairs card and the diagnostic entities are always raised, so nothing is hidden — you just are not interrupted.",
          "notify_services": "Leave empty and alerts still reach every administrator as a persistent notification inside Home Assistant."
        }
      },
      "ingress": {
        "title": "Can anyone still get in?",
        "description": "**Your cluster can fail over perfectly and still leave you locked out.** That is not hypothetical: it happened on the reference pair. The lease moved, the radios followed, Home Assistant came up healthy and ran the house — and the address on the phone still pointed at the machine that had died and returned **502**. Every check the cluster performed was green, because every check the cluster performed looked inwards.\n\nGive it the address **you** type, and whichever node holds the lease will request it once a minute and tell you when it stops answering.\n\n### 🚨 What a green reading does not prove\n\nThe request is made from inside Home Assistant, on the node itself, on your own network. So:\n\n- **It cannot prove somebody outside can reach you.** If you use split-horizon DNS — normal for anyone reaching Home Assistant through a tunnel — this hostname resolves to a local address from in here and to your tunnel from a phone on mobile data. The check then tests a path no outside user ever takes, and stays green while the tunnel is down. No probe launched from inside a network can test the path that starts outside it. For that half, use an external uptime monitor.\n- **It cannot prove *this* node answered.** Something at that address responded. Behind a proxy, or in a warm-standby pair, that may have been the other node.\n- **It cannot prove you can log in.** Redirects are not followed, so an SSO challenge counts as the door working.\n\nA **red** reading is the trustworthy direction: if this cannot reach the address from a machine on the same network as the service, nobody further away is getting in either.\n\nNone of the infrastructure — DNS, proxies, tunnels, floating addresses — is moved by this integration, and it never will be. See `GUIDE-ingress.md`, which carries its own no-warranty notice. This page only makes the cluster **look**.",
        "data": {
          "ingress_url": "Your Home Assistant address",
          "ingress_verify_tls": "Check the certificate"
        },
        "data_description": {
          "ingress_url": "The whole address including the scheme, as in {example_url}. Leave it empty to switch the check off entirely — that is the default, and no entity or alert is created. 401 and 403 count as reachable: being refused proves something is there to refuse you.",
          "ingress_verify_tls": "Turn this off if you terminate TLS on a certificate Home Assistant has no reason to trust — an internal CA or a self-signed one. The check still answers “did anything answer at that address”; it just stops asking who. Better than a check that is red for ever and gets switched off along with the real alarm."
        }
      },
      "replication": {
        "title": "What crosses to the standby",
        "description": "These four settings decide whether an entity's **live value** is mirrored and restored on the node that takes over. They apply in the order shown, and an exclusion always beats an inclusion.\n\n**Domains** is an allowlist — anything not listed does not cross. The defaults are conservative on purpose: stateful things a promoted node cannot recompute, and nothing whose restore could actuate hardware.\n\n**Adding `light` or `switch` does not do what it looks like.** Verified against Home Assistant's own source: the restore runs on `EVENT_HOMEASSISTANT_START`, and an automation attaches its triggers on `EVENT_HOMEASSISTANT_STARTED` — which fires *after*. So a restored value at boot cannot fire your automations; they are not listening yet. That is Home Assistant's own behaviour and this integration deliberately matches it rather than working around it.\n\n`sensor` and `binary_sensor` are absent deliberately: their values are re-read from the device on the new node within seconds, so a restored reading is stale before anyone sees it — and they are the bulk of all state churn. If one genuinely cannot be re-derived, add that single entity below rather than the whole domain.\n\n**This page is not about your configuration.** Entity and device *settings* — enabled, hidden, area, renames — live in `.storage` and are replicated in full by the fileset regardless of anything here.\n\n**`automation` is a special case, and a useful one.** Whether an automation is enabled is a decision you made, and it crosses nowhere by default — a promoted node restores it from *its own* memory of whenever it last ran. Adding `automation` here makes it cross, and it is applied by calling `automation.turn_on`/`turn_off` rather than by writing the state, because an automation's component owns its own state: writing it would show \"off\" while the automation kept firing.\n\n🚨 **And for `light`, `switch` and other device-backed domains the restore does not actually work.** It writes Home Assistant's state machine; it does not command the device. The integration that owns the light still believes what it believed, and corrects the value the next time it reports — so you get a reading that flips, fires your automations on the way past, and then reverts. The risk without the benefit.\n\n`automation` is the exception, and only because it is applied by calling `automation.turn_on`/`turn_off` rather than by writing state. Extending that to `light` or `switch` would mean a promotion switching real things on in your house, which is a decision this integration will not make for you.",
        "data": {
          "include_domains": "Domains that cross",
          "include_entities": "Also cross these specific entities",
          "exclude_entities": "Never cross these entities",
          "exclude_devices": "Never cross anything from these devices"
        },
        "data_description": {
          "include_domains": "Offered from the domains this instance actually has, plus the defaults. You can type one that is not listed yet.",
          "include_entities": "For a single entity in a domain you do not want wholesale — a utility meter total, a manually-set counter.",
          "exclude_entities": "Beats everything above. Use for one entity that is specific to this machine.",
          "exclude_devices": "Shorthand for excluding every entity a device exposes, including ones it gains later. The right tool for hardware wired to one node — a radio that does not fail over."
        }
      }
    },
    "error": {
      "invalid_ingress_url": "That is not an address this can request. It needs both a scheme and a host name — the field description above shows the shape."
    }
  },
  "entity": {
    "sensor": {
      "entities_tracked": {
        "name": "Entities tracked"
      },
      "last_snapshot_age": {
        "name": "Last snapshot age"
      },
      "entities_restored": {
        "name": "Entities restored"
      },
      "fileset_age": {
        "name": "Fileset age"
      },
      "cluster_leader": {
        "name": "Cluster leader"
      },
      "shared_snapshot_age": {
        "name": "Shared snapshot age"
      },
      "cluster_members": {
        "name": "Cluster members"
      },
      "clock_skew": {
        "name": "Clock skew"
      },
      "unreplicated_references": {
        "name": "Unreplicated config references"
      },
      "radio_silence": {
        "name": "Radio silence"
      },
      "oversized_entities": {
        "name": "Oversized entities"
      },
      "recorder_snapshot_age": {
        "name": "Recorder snapshot age"
      },
      "statistics_age": {
        "name": "Statistics replication age"
      },
      "replicated_domains": {
        "name": "Replicated domains"
      },
      "replication_exclusions": {
        "name": "Replication exclusions"
      }
    },
    "binary_sensor": {
      "backend": {
        "name": "Backend"
      },
      "fileset_degraded": {
        "name": "Fileset degraded"
      },
      "is_leader": {
        "name": "Is leader"
      },
      "maintenance_hold": {
        "name": "Maintenance hold"
      },
      "ingress_reachable": {
        "name": "Ingress reachable"
      }
    },
    "button": {
      "statistics_seed": {
        "name": "Write statistics seed"
      }
    }
  },
  "selector": {
    "leadership_source": {
      "options": {
        "always": "Always leader (single node / cold standby)",
        "entity": "Follow a Home Assistant entity (Keepalived toggles it)",
        "lease": "Valkey lease (strongest — guards against split-brain)"
      }
    },
    "topology_model": {
      "options": {
        "cold": "Cold standby — recommended. HA stopped on the standby until promotion. Simplest and safest; costs cold-boot RTO.",
        "warm": "Warm standby — HA running but neutered. Seconds-level failover; needs the firewall bundle and a real leadership signal."
      }
    },
    "docker_network": {
      "options": {
        "host": "network_mode: host — best mDNS/HomeKit, filtered by uid",
        "macvlan": "macvlan — container has its own LAN IP; needs the netdev/egress hook",
        "bridge": "bridge — cleanest filtering via DOCKER-USER, needs an mDNS reflector",
        "all": "Not sure — generate all three and choose at install time"
      }
    },
    "ha_start_mode": {
      "options": {
        "docker": "docker start / docker stop (default)",
        "compose": "docker compose (one service)"
      }
    },
    "history_database": {
      "options": {
        "shared": "Shared — one database both machines use (PostgreSQL / MariaDB)",
        "dedicated": "Dedicated — each machine keeps its own SQLite file"
      }
    },
    "notify_conditions": {
      "options": {
        "promoted": "Failover — the house has moved to the other node",
        "restored_nothing": "Failover restored NOTHING from the other node",
        "recovered": "Recovery — a condition below has cleared",
        "backend_lost": "Valkey unreachable — the cluster can no longer fail over",
        "fileset_degraded": "Promoted with an incomplete configuration",
        "statistics_schema_mismatch": "Statistics replication has stopped (schema)",
        "statistics_gap": "Statistics history has a gap",
        "statistics_not_seeded": "Statistics have never been seeded",
        "statistics_stalled": "Statistics replication has stalled",
        "ingress_unreachable": "Nobody can get in — your Home Assistant address stopped answering",
        "devices_disabled": "Promoted with hardware missing, so entries were switched off"
      }
    }
  },
  "services": {
    "flush_snapshot": {
      "name": "Flush snapshot",
      "description": "Write this node's entity state to the shared store now. Refuses on a node that does not hold cluster leadership."
    },
    "clear_degraded": {
      "name": "Clear the degraded marker",
      "description": "Acknowledge the degraded go-bag marker and remove its repair issue. Fixes nothing on its own; the marker returns if the condition persists."
    }
  }
}
