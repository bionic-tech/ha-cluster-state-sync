{
  "config": {
    "step": {
      "user": {
        "title": "Before you install",
        "description": "**This is alpha software and there are no guarantees that it runs.** It is shared with the Home Assistant community for testing and feedback, and no guarantees are given on data integrity or on consistency of functionality at this time.\n\n**It restores state to devices that act in the physical world.** The default mirrored domains include `climate`, `water_heater`, `vacuum` and `timer` — so a stale or wrong restore can change your heating setpoint or hot water, not just a value on a dashboard. `alarm_control_panel`, `person` and `device_tracker` are **opt-in and off by default**; if you turn them on, an alarm's state is mirrored too.\n\nProvided as is, without warranty and without liability. Prove a failover on a system you do not depend on, before you depend on one.",
        "data": {
          "accept_risk": "I understand and accept the risk"
        }
      },
      "direct": {
        "title": "Direct Redis / Valkey connection",
        "data": {
          "redis_host": "Host",
          "redis_port": "Port",
          "redis_username": "Username (leave blank if the server has no ACL user)",
          "redis_password": "Password (leave blank if none)",
          "redis_db": "Database number",
          "cluster_namespace": "Cluster namespace (must match across nodes)",
          "node_id": "This node's identifier",
          "cluster_secret": "Cluster secret (must be identical on both nodes)",
          "redis_use_tls": "Use TLS",
          "redis_tls_ca_certs": "TLS CA certificate path (blank = system trust store)",
          "snapshot_interval": "Snapshot interval (seconds)",
          "restore_max_age": "Maximum snapshot age to restore (seconds)"
        },
        "data_description": {
          "cluster_secret": "Signs every snapshot entry so a node only accepts state written by its peer. Copy this exact value to the other node — a mismatch means neither will restore from the other. Without it, nothing is restored at all.",
          "redis_use_tls": "Strongly recommended if you mirror person, device_tracker or alarm_control_panel entities, which otherwise cross the network in the clear.",
          "leadership_source": "Only the leader writes to the shared snapshot. 'always' suits a single node or a cold standby whose HA is stopped. 'entity' follows a flag your Keepalived notify_master/notify_backup scripts toggle. 'lease' takes a TTL lease in Valkey and is the strongest option — it is the split-brain guard, since VRRP alone can dual-master under a partition."
        }
      },
      "sentinel": {
        "title": "Sentinel-backed Redis / Valkey",
        "data": {
          "redis_sentinel_hosts": "Sentinel hosts (comma-separated host:port)",
          "redis_sentinel_service": "Sentinel master service name",
          "redis_username": "Username (leave blank if the server has no ACL user)",
          "redis_password": "Password (leave blank if none)",
          "redis_db": "Database number",
          "cluster_namespace": "Cluster namespace (must match across nodes)",
          "node_id": "This node's identifier",
          "cluster_secret": "Cluster secret (must be identical on both nodes)",
          "redis_use_tls": "Use TLS",
          "redis_tls_ca_certs": "TLS CA certificate path (blank = system trust store)",
          "snapshot_interval": "Snapshot interval (seconds)",
          "restore_max_age": "Maximum snapshot age to restore (seconds)"
        },
        "data_description": {
          "cluster_secret": "Signs every snapshot entry so a node only accepts state written by its peer. Copy this exact value to the other node — a mismatch means neither will restore from the other. Without it, nothing is restored at all.",
          "redis_use_tls": "Strongly recommended if you mirror person, device_tracker or alarm_control_panel entities, which otherwise cross the network in the clear.",
          "leadership_source": "Only the leader writes to the shared snapshot. 'always' suits a single node or a cold standby whose HA is stopped. 'entity' follows a flag your Keepalived notify_master/notify_backup scripts toggle. 'lease' takes a TTL lease in Valkey and is the strongest option — it is the split-brain guard, since VRRP alone can dual-master under a partition."
        }
      },
      "topology": {
        "title": "Standby model",
        "description": "Cold is recommended: the standby's Home Assistant is stopped, so there are no side effects to suppress. Warm fails over in seconds but needs the firewall bundle.",
        "data": {
          "topology_model": "Standby model",
          "leadership_source": "Leadership signal",
          "leadership_entity": "Leadership entity (only for the 'entity' signal)",
          "gate_recorder": "Stop recorder writes on the follower (needs a shared recorder database)",
          "gate_automations": "Stop automations on the follower (warm standby only)",
          "ha_config_path": "Home Assistant config path ON THE HOST (not /config)",
          "ha_container": "Home Assistant container name"
        }
      },
      "warm": {
        "title": "Warm standby — network gating",
        "description": "Used to generate the nftables/iptables rules the follower loads. These rules are generated without access to your hosts and are not validated — read them before loading.",
        "data": {
          "iot_subnets": "IoT subnet(s), comma-separated CIDR",
          "block_discovery": "Block mDNS (5353) and SSDP (1900) on the follower",
          "docker_network": "Docker network mode",
          "ha_uid": "UID Home Assistant runs as",
          "ha_container_ip": "Container IP (macvlan/bridge only)",
          "settle_delay": "Promotion settle delay (seconds)"
        }
      },
      "fileset": {
        "title": "Config replication",
        "description": "Everything `configuration.yaml` includes is replicated automatically — `configs/`, `themes/`, `packages/` and so on are followed, so you do not list them here.\n\nThis is for what no parser can see: files an integration opens by path at runtime. Anything found in your config directory is offered below; you can also type a path.",
        "data": {
          "fileset_enabled": "Replicate the config fileset to the standby",
          "fileset_hot_interval": "How often to publish .storage (seconds)",
          "fileset_exclusions": "Filename patterns to skip",
          "fileset_extra_paths": "Also replicate these (tick what a promoted standby needs)",
          "fileset_max_bytes": "Refuse to publish above this many bytes",
          "fileset_stale_after": "Warn if the standby's copy is older than (seconds)",
          "fileset_extra_custom": "Additional paths not listed above (type and press enter)"
        }
      },
      "bundle": {
        "title": "Host bundle generated",
        "description": "Written to:\n`{bundle_path}`\n\nModel: **{model}**\n\n{file_list}\n\n{next_steps}",
        "menu_options": {
          "transfer": "Show me how to copy these onto this host",
          "finish": "Finish — I'll handle the files myself"
        }
      },
      "choose": {
        "title": "How does this node reach Valkey?",
        "description": "**Direct** is what almost everyone wants: one Valkey or Redis server, reached by host and port.\n\n**Sentinel** is for a Redis Sentinel cluster that elects its own primary. The code path exists but is **deferred and untested** — choose it only if you already run Sentinel and are willing to be the first to prove it.",
        "menu_options": {
          "direct": "Direct — a single Valkey or Redis server",
          "sentinel": "Sentinel — a Redis Sentinel cluster (untested)"
        }
      },
      "transfer": {
        "title": "Copying the bundle onto this host",
        "description": "The bundle was written **inside** Home Assistant's container. It has to reach `/etc/cluster-sync` on the host this container runs on.\n\nLeave all three blank if you are already at a terminal on that host.\n\n🚨 This is about **this** node only. Nine of these files carry this node's identity — installing them on the other node would make it claim this one's id, and both would then believe they lead. The other node runs its own wizard.\n\nNone of these answers is saved.",
        "data": {
          "host": "Host address (blank if you are on it already)",
          "user": "SSH username (blank for your current one)",
          "key_path": "SSH key path (blank for your default, or if you use a password)"
        }
      },
      "transfer_commands": {
        "title": "Commands to run",
        "description": "{commands}\n\nSubmit to go back."
      },
      "domains": {
        "title": "What crosses to the standby",
        "description": "Which entity domains are mirrored and restored on the node that takes over.\n\nThe defaults are conservative on purpose: stateful things a promoted node needs to know, and nothing whose restore could actuate hardware.\n\n**Adding `light` or `switch` is a real decision.** Restoring one writes a state change, and a state change is what automations trigger on — so a restored value can fire an automation and turn real things on. Check what triggers on them before adding them.\n\n`person`, `device_tracker` and `alarm_control_panel` also tell anyone who can read the shared store when the house is empty. Pair them with TLS and a dedicated user.",
        "data": {
          "include_domains": "Entity domains to mirror",
          "include_entities": "Individual entities to mirror as well (optional)"
        },
        "data_description": {
          "include_domains": "Leave empty to use the built-in defaults.",
          "include_entities": "Full entity IDs, for one-off entities in a domain you do not want wholesale. They need not exist yet."
        }
      },
      "container": {
        "title": "How this node starts Home Assistant",
        "description": "**`docker` — the default, and right for most installs.** The promoter runs `docker start` / `docker stop` on one container. Fast, needs no project file, and nothing else in your estate can break it.\n\n**`compose` — when nobody starts the container by hand.** Home Assistant is one service in a compose project, possibly behind a profile. The promoter acts on that service alone (`up -d --no-deps`), never the whole estate, and stops it with `stop` — never `down`, which would remove the container a demoted node needs in order to be promoted again.\n\n🚨 **Compose mode needs an env file.** Compose interpolates the *entire* project file before it filters by profile or service, so one unset variable in a service you are not touching aborts the command. The promoter runs from systemd with none of your shell environment — without an env file it fails at exactly the moment it is needed.",
        "data": {
          "ha_start_mode": "Start method",
          "compose_file": "Compose file",
          "compose_service": "Service name",
          "compose_profile": "Profile (if the service is behind one)",
          "compose_env_file": "Env file to source before every compose call"
        },
        "data_description": {
          "compose_file": "Full path on the host, e.g. /srv/docker/compose.yml",
          "compose_service": "The service name in the file, not the container name.",
          "compose_profile": "Leave empty if you do not use profiles.",
          "compose_env_file": "Strongly recommended. Sourced and exported before compose runs."
        }
      }
    },
    "error": {
      "cannot_connect": "Could not reach the backend with these settings.",
      "invalid_namespace": "Namespace must be lowercase letters, digits, '_' or '-' only.",
      "fileset_needs_secret": "A cluster secret is required to encrypt the replicated fileset. Set one on the backend step before enabling this.",
      "must_accept": "You must accept before continuing. This software carries no warranty."
    },
    "abort": {
      "already_configured": "This node is already configured for this cluster namespace.",
      "reconfigure_successful": "Cluster State Sync reconfigured. The host bundle has been regenerated — reinstall it on this host if any host-side value changed."
    }
  },
  "issues": {
    "fileset_degraded": {
      "title": "Standby config fileset is degraded",
      "description": "The host-side swap could not put a fresh copy of the config fileset in place before this instance started (reason: {reason}). Decision D4 promotes anyway rather than refuse to boot, so Home Assistant is running, but integrations, devices, and the mobile app's login may be missing or out of date. Check the swap log on the host, and reload this integration once a fresh fileset has been staged and swapped in."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Cluster State Sync options",
        "data": {
          "redis_host": "Host",
          "redis_port": "Port",
          "redis_password": "Password (leave blank if none)",
          "redis_db": "Database number",
          "redis_sentinel_hosts": "Sentinel hosts (comma-separated host:port)",
          "redis_sentinel_service": "Sentinel master service name",
          "cluster_namespace": "Cluster namespace",
          "node_id": "This node's identifier",
          "cluster_secret": "Cluster secret (must be identical on both nodes)",
          "redis_use_tls": "Use TLS",
          "redis_tls_ca_certs": "TLS CA certificate path (blank = system trust store)",
          "leadership_source": "Leadership signal",
          "leadership_entity": "Leadership entity (only for the 'entity' signal)",
          "gate_recorder": "Stop recorder writes on the follower (needs a shared recorder database)",
          "gate_automations": "Stop automations on the follower (warm standby only)",
          "snapshot_interval": "Snapshot interval (seconds)",
          "restore_max_age": "Maximum snapshot age to restore (seconds)"
        },
        "data_description": {
          "cluster_secret": "Signs every snapshot entry so a node only accepts state written by its peer. Copy this exact value to the other node — a mismatch means neither will restore from the other. Without it, nothing is restored at all.",
          "redis_use_tls": "Strongly recommended if you mirror person, device_tracker or alarm_control_panel entities, which otherwise cross the network in the clear.",
          "leadership_source": "Only the leader writes to the shared snapshot. 'always' suits a single node or a cold standby whose HA is stopped. 'entity' follows a flag your Keepalived notify_master/notify_backup scripts toggle. 'lease' takes a TTL lease in Valkey and is the strongest option — it is the split-brain guard, since VRRP alone can dual-master under a partition."
        }
      }
    }
  },
  "entity": {
    "sensor": {
      "entities_tracked": {
        "name": "Entities tracked"
      },
      "last_snapshot_age": {
        "name": "Last snapshot age"
      },
      "entities_restored": {
        "name": "Entities restored"
      },
      "fileset_age": {
        "name": "Fileset age"
      },
      "cluster_leader": {
        "name": "Cluster leader"
      },
      "shared_snapshot_age": {
        "name": "Shared snapshot age"
      },
      "cluster_members": {
        "name": "Cluster members"
      },
      "clock_skew": {
        "name": "Clock skew"
      },
      "unreplicated_references": {
        "name": "Unreplicated config references"
      }
    },
    "binary_sensor": {
      "backend": {
        "name": "Backend"
      },
      "fileset_degraded": {
        "name": "Fileset degraded"
      },
      "is_leader": {
        "name": "Is leader"
      },
      "maintenance_hold": {
        "name": "Maintenance hold"
      }
    }
  },
  "selector": {
    "leadership_source": {
      "options": {
        "always": "Always leader (single node / cold standby)",
        "entity": "Follow a Home Assistant entity (Keepalived toggles it)",
        "lease": "Valkey lease (strongest — guards against split-brain)"
      }
    },
    "topology_model": {
      "options": {
        "cold": "Cold standby — recommended. HA stopped on the standby until promotion. Simplest and safest; costs cold-boot RTO.",
        "warm": "Warm standby — HA running but neutered. Seconds-level failover; needs the firewall bundle and a real leadership signal."
      }
    },
    "docker_network": {
      "options": {
        "host": "network_mode: host — best mDNS/HomeKit, filtered by uid",
        "macvlan": "macvlan — container has its own LAN IP; needs the netdev/egress hook",
        "bridge": "bridge — cleanest filtering via DOCKER-USER, needs an mDNS reflector",
        "all": "Not sure — generate all three and choose at install time"
      }
    },
    "ha_start_mode": {
      "options": {
        "docker": "docker start / docker stop (default)",
        "compose": "docker compose (one service)"
      }
    }
  },
  "services": {
    "flush_snapshot": {
      "name": "Flush snapshot",
      "description": "Write this node's entity state to the shared store now. Refuses on a node that does not hold cluster leadership."
    },
    "clear_degraded": {
      "name": "Clear the degraded marker",
      "description": "Acknowledge the degraded go-bag marker and remove its repair issue. Fixes nothing on its own; the marker returns if the condition persists."
    }
  }
}
